<form action="{{ path('fos_user_profile_edit') }}" {{ form_enctype(form) }} method="POST" class="fos_user_profile_edit edit-profil">
    {{ form_widget(form) }}
    <div class="text-right align-right">
        <input type="submit" value="{{ 'profile.edit.submit'|trans({}, 'FOSUserBundle') }}" class="red-button" />
    </div>
</form>
