<div class="main row">
    <div class="large-10 medium-12 small-18 columns login-container">
        <h2 class="text-center">S'inscrire</h2>

<form action="{{ path('fos_user_registration_register') }}" {{ form_enctype(form) }} method="POST" class="fos_user_registration_register register">
    {{ form_widget(form) }}
    <div class="row align-center text-center">
        <input type="submit" value="{{ 'registration.submit'|trans({}, 'FOSUserBundle') }}" class="red-button"/>
    </div>
</form>
        
    </div>
</div>
